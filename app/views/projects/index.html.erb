<h1>PROJECTS</h1>

<% @projects.each do |project| %>
	<ul>
		<li> <%=  link_to(project.title, project_path(project.id)) %> </li>
		<li>Updated at: <%= project.updated_at.strftime("%H:%M:%S") %> </li>
	</ul>
<% end %>

<%= link_to "Create Project", new_project_path %>